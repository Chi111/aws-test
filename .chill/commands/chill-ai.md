# /chill-ai

Use this project command as the friendly workflow entry point.

For this MVP, continue from:

```text
specs/1-github-profile-admin-sam/tasks.md
```

The implementation default is React/Vite + Hono + Drizzle + PostgreSQL + SAM, scaffolded from better-t-stack.
