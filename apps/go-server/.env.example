PORT=8080
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres?sslmode=disable
DATABASE_MAX_CONNS=5
DATABASE_MIN_CONNS=0
# AWS: mount/copy the RDS CA bundle and set this path to enforce certificate verification.
# DATABASE_SSL_CA_PATH=/etc/ssl/certs/aws-rds-global-bundle.pem
CORS_ORIGIN=http://localhost:3001

